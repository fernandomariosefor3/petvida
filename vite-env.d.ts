/// <reference types="vite/client" />

// Injected by vite.config.ts via `define` — not a real env var, so it needs
// its own ambient declaration for TypeScript to see it.
declare const __BASE_PATH__: string;
